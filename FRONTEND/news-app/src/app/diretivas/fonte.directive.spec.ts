import { FonteDirective } from './fonte.directive';

describe('FonteDirective', () => {
  it('should create an instance', () => {
    const directive = new FonteDirective();
    expect(directive).toBeTruthy();
  });
});
